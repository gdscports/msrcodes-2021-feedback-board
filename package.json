{
  "name": "fbac-monorepo",
  "private": true,
  "scripts": {
    "bootstrap": "yarn; lerna bootstrap;",
    "lint": "lerna run lint",
    "pkg:api": "lerna run --scope api --stream",
    "pkg:web": "lerna run --scope web --stream",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^13.1.0",
    "@commitlint/config-angular": "^13.1.0",
    "lerna": "^4.0.0",
    "husky": ">=6"
  }
}
